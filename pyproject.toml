[project]
name = "regulation-compliance-agent"
version = "0.1.0"
requires-python = ">=3.11"

dependencies = [
    "httpx",
    "langchain-core",
    "langchain-openai",
    "openai",
    "pydantic>=2",
    "pypdf",
    "python-dotenv",
    "tqdm",
]

[project.optional-dependencies]
dev = [
    "pytest",
]
